<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="PermissionDialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
    </style>
    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="My_Widget.MaterialComponents.Chip.Choice" parent="Widget.MaterialComponents.Chip.Choice">
        <!-- Chip background color selector -->
        <item name="chipBackgroundColor">@color/chip_background</item>

        <!-- Chip text color selector -->
        <item name="android:textColor">@color/black</item>
        <!-- Chip close icon color selector -->
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="fontFamily">@font/product_sans_regular</item>
    </style>

</resources>